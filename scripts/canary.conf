# Dynamic canary traffic configuration
# This file is updated by update-canary-weight.sh or monitor-canary.sh
# Default: 0% traffic to canary (all to production)

set $target production;
set $canary_weight 0;
